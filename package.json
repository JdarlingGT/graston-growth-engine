{
  "name": "gt-archived-data",
  "private": true,
  "version": "1.0.0",
  "workspaces": ["apps/*"],
  "scripts": {
    "build": "npm run build --workspace=frontend",
    "dev": "npm run dev --workspace=frontend"
  },
  "devDependencies": {
    "prisma": "^4.0.0",
    "vite": "^4.0.0"
  }
}
